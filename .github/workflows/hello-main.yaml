name: hello-main
on:
  push:
    branches:
      - main
jobs:
  main-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello-main
        env:
          webname: ${{ secrets.SECRET_WEBNAME }}
        run: echo "Hello $webname"
